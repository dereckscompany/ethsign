% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eip712.R
\name{eip712_digest}
\alias{eip712_digest}
\title{Compute an EIP-712 Typed-Data Signing Digest}
\usage{
eip712_digest(domain, primary_type, types, message)
}
\arguments{
\item{domain}{Named list; the EIP-712 domain, with \code{name} (character),
\code{version} (character), \code{chainId} (numeric or \code{gmp::bigz}), and
\code{verifyingContract} (a \verb{0x}-prefixed 20-byte address).}

\item{primary_type}{Character; the struct name, e.g. \code{"Order"}.}

\item{types}{Unnamed list; the ordered field list for \code{primary_type}, each
element a \verb{list(name = <chr>, type = <chr>)} in definition order.}

\item{message}{Named list; field values, looked up by field name.}
}
\value{
\code{raw(32)}; the signing digest.
}
\description{
Hashes a single EIP-712 struct under its domain and returns the
\code{0x1901}-prefixed digest ready to sign (the value an \link{EthSigner} passes to
\verb{$sign_digest()}). This is the standard typed-data digest used by EVM venues
such as Hyperliquid and Polymarket.
}
\details{
Scope: a single struct over atomic field types (\code{string}, \code{uint64},
\code{uint256}, \code{bool}, \code{address}, \code{bytes32}). Nested struct types and arrays are
out of scope for v0.
}
\examples{
domain <- list(
  name = "Exchange",
  version = "1",
  chainId = 1337,
  verifyingContract = "0x0000000000000000000000000000000000000000"
)
types <- list(
  list(name = "source", type = "string"),
  list(name = "connectionId", type = "bytes32")
)
message <- list(source = "a", connectionId = keccak256("hello"))
eip712_digest(domain, "Agent", types, message)

}
