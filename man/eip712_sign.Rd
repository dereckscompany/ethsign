% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eip712.R
\name{eip712_sign}
\alias{eip712_sign}
\title{Sign EIP-712 Typed Data and Return the 65-Byte Hex Signature}
\usage{
eip712_sign(signer, domain, types, primary_type, message)
}
\arguments{
\item{signer}{(class<EthSigner>) the wallet signer.}

\item{domain}{(list) the EIP-712 domain; see \code{\link[=eip712_digest]{eip712_digest()}}.}

\item{types}{(list) the field list for \code{primary_type}, or a struct map for
nested structs; see \code{\link[=eip712_digest]{eip712_digest()}}.}

\item{primary_type}{(scalar<character>) the struct name.}

\item{message}{(list) the named field values.}
}
\value{
(scalar<character>) a \verb{0x}-prefixed 130-hex-character signature
(\code{r(32) || s(32) || v(1)}, \code{v} = 27 or 28).
}
\description{
Convenience wrapper for venues (e.g. Polymarket) that want the wire
signature directly: hashes \code{message} via \code{\link[=eip712_digest]{eip712_digest()}}, signs it with
\code{signer}, and returns the 65-byte \code{r(32) || s(32) || v(1)} hex form (see
\code{\link[=as_hex]{as_hex()}}).
}
\examples{
signer <- eth_signer_random()
domain <- list(
  name = "Exchange",
  version = "1",
  chainId = 1337,
  verifyingContract = "0x0000000000000000000000000000000000000000"
)
types <- list(list(name = "source", type = "string"))
eip712_sign(signer, domain, types, "Agent", list(source = "a"))

}
