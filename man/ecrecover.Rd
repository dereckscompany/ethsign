% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/secp256k1.R
\name{ecrecover}
\alias{ecrecover}
\title{Recover the Signing Address from an ECDSA Signature}
\usage{
ecrecover(digest32, r, s, v)
}
\arguments{
\item{digest32}{\code{raw(32)}; the signed message digest.}

\item{r, s}{Signature scalars; a \code{gmp::bigz}, integer, \verb{0x}-prefixed hex
string, or \code{raw(32)} (e.g. the \code{r}/\code{s} of \code{\link[=as_rsv]{as_rsv()}}).}

\item{v}{Integer; the recovery byte, \code{27} or \code{28}.}
}
\value{
Character; the recovered lowercase \verb{0x}-prefixed 20-byte address, or
\code{NULL} if recovery fails.
}
\description{
Ethereum's \code{ecrecover}: given the signed digest and the \verb{(r, s, v)}
signature, recovers the Ethereum address that produced it. This is the
inverse check against an \link{EthSigner}'s output, e.g. verifying a SIWE login.
}
\examples{
priv <- "0x0123456789012345678901234567890123456789012345678901234567890123"
signer <- eth_signer(priv)
digest <- keccak256("recover me")
sig <- signer$sign_digest(digest)
rsv <- as_rsv(sig)
ecrecover(digest, rsv$r, rsv$s, rsv$v) == signer$address

}
